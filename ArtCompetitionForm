<html>
    <title>Submission Form</title>
    <head>
        <style>
            body
            {
                background-color: #CBC3E3;

            }
            .inp
            {
                font-family:'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
                text-align: center;
                width: 50%;
                background-color:whitesmoke;
                border-top: #80108f 50px solid;
                margin-left: 400px;
                border-left: #80108f 4px solid;
                border-right: #80108f 4px solid;
                border-bottom: #80108f 10px solid;
            }
            input[type="submit"]
            {

              background-color: #80108f;
              font-size: 17px;
              color: white;
              padding: 1%;
            }
        </style>
         <script>
            function validateForm() {
                // Get form inputs by their IDs
                var fn = document.getElementById("fn").value;
                var ln = document.getElementById("ln").value;
                var email = document.getElementById("email").value;
                var phno = document.getElementById("phno").value;

                // Perform validation checks
                if (fn == "" || ln == "") {
                    alert("Please enter your full name.");
                    return false;
                }
                if (email.indexOf("@") == -1 || email.indexOf(".") == -1) {
                    alert("Please enter a valid email address.");
                    return false;
                }
                if (phno.length < 10) {
                    alert("Please enter a valid phone number.");
                    return false;
                }

                // If all checks pass, return true to submit the form
                return true;
            }
        </script>
        </head>
